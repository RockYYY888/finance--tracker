services:
  backend:
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      http_proxy: ${ASSET_TRACKER_HTTP_PROXY:-http://host.docker.internal:10808}
      https_proxy: ${ASSET_TRACKER_HTTPS_PROXY:-http://host.docker.internal:10808}
      HTTP_PROXY: ${ASSET_TRACKER_HTTP_PROXY:-http://host.docker.internal:10808}
      HTTPS_PROXY: ${ASSET_TRACKER_HTTPS_PROXY:-http://host.docker.internal:10808}
      no_proxy: ${ASSET_TRACKER_NO_PROXY:-localhost,127.0.0.1,backend,frontend,caddy}
      NO_PROXY: ${ASSET_TRACKER_NO_PROXY:-localhost,127.0.0.1,backend,frontend,caddy}
