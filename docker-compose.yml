services:
  backend:
    build:
      context: ./backend
    environment:
      ASSET_TRACKER_API_TOKEN: ${ASSET_TRACKER_API_TOKEN}
      ASSET_TRACKER_APP_ENV: production
      ASSET_TRACKER_PUBLIC_ORIGIN: ${ASSET_TRACKER_PUBLIC_ORIGIN}
      ASSET_TRACKER_SESSION_SECRET: ${ASSET_TRACKER_SESSION_SECRET}
    restart: unless-stopped
    volumes:
      - ./backend/data:/app/data

  frontend:
    build:
      context: ./frontend
      args:
        VITE_API_BASE_URL: ""
        VITE_ENABLE_PWA: "true"
    restart: unless-stopped

  caddy:
    image: caddy:2.10-alpine
    depends_on:
      - backend
      - frontend
    environment:
      ASSET_TRACKER_API_TOKEN: ${ASSET_TRACKER_API_TOKEN}
      ASSET_TRACKER_DOMAIN: ${ASSET_TRACKER_DOMAIN:-localhost}
    ports:
      - "80:80"
      - "443:443"
    restart: unless-stopped
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config

volumes:
  caddy_config:
  caddy_data:
